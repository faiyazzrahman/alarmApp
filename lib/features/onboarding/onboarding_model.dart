class OnboardingModel {
  final String title;
  final String description;
  final String mediaPath; // works for image or video

  OnboardingModel({
    required this.title,
    required this.description,
    required this.mediaPath,
  });
}
